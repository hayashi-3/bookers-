<% @books.each do |book| %>
<h4>Title</h4>
 <%= @book.title %>
<h4>Body</h4>
 <%= @book.body %>
<span>
<%= link_to "Edit", edit_books_path(@book) %>
<%= link_to "Back", books_path(@book) %>
</span>
<% end %>